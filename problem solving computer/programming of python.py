# Ask Input from user 
obtained_marks = float(input("Enter obtained_marks:"))
total_marks = float(input("Enter total marks:"))

# calculate percentage 
percentage = (obtained_marks / total_marks ) * 100

# Display result
print("percentage:", percentage, "%")
